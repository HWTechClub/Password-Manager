<AppWidget>:
    canvas:
        Rectangle :
            pos : self.center_x * 4/3,0 #x,y coordinates
            size: 10,self.height #Width,height
    
    Label:
        font_size : 20
        center_x: root.width/3 #the width of the window / 4, so, centering thge elementys on the screen.
        top: root.top - 20
        text: "Your profile"

    Label:
        font_size : 20
        center_x: root.width * 5/6 #the width of the window / 4, so, centering thge elementys on the screen.
        top: root.top - 20
        text: "Passwords"